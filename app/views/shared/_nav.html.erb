<header class='text-gray-600 body-font border-b-2 border-b-light'>
    <div class='container mx-auto max-w-7xl flex flex-wrap p-2 flex-col md:flex-row items-center'>
        
        <%= link_to root_path, class: 'flex title-font font-medium items-center md:justify-start justify-center text-gray-900' do %>
            <%= image_tag('logo1x1.png') %>
            <span class='ml-3 text-xl'>COMPANY</span>
        <% end %>
        
        <nav class='md:mr-auto md:ml-4 md:py-1 md:pl-4 md:border-l flex flex-wrap items-center text-base justify-center'>
            <a href='#' class='mr-5'>Link</a>
            <a href='#' class='mr-5'>Link</a>
            <a href='#' class='mr-5'>Link</a>
            <a href='#' class='mr-5'>Link</a>
        </nav>

        <div class='space-x-2 lg:justify-end'>
            <% if signed_in? %>
                <%= link_to 'Profile', profile_path, class: 'text-sm leading-6 text-gray-900' %>
                <%= link_to 'Sign Out', logout_path, class: 'text-sm leading-6 text-gray-900' %>
            <% else %>
                <%= link_to 'Log In', login_path, class: 'text-sm leading-6 text-gray-900' %>
            <% end %>
        
        </div>

    </div>
</header>
